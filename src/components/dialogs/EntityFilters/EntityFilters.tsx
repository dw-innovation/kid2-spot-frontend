import React from "react";

import Dialog from "../Dialog";

const DIALOG_NAME = "entityFilters";

const EntityFilters = () => {
  return <Dialog dialogName={DIALOG_NAME}>EntityFilters</Dialog>;
};

export default EntityFilters;
