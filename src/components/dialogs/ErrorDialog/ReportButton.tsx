import React from "react";

import { Button } from "@/components/ui/button";

const ReportButton = () => {
  return (
    <Button variant={"secondary"} className="w-fit">
      Report Incorrect Entities
    </Button>
  );
};

export default ReportButton;
